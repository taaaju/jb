package oxygen.services.jobs_processor.integrations;

// this is not running yet as we do not have the full spring context!!! TODO fix this
//@Testcontainers
//@Import({OxyJobSubmissionService.class})
//@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
//class OxyJobSubmissionServiceTest extends BaseIntegrationTest {
class OxyJobSubmissionServiceTest {
/*
    @Autowired
    private OxyJobSubmissionService oxyJobSubmissionService;

    @Test
    @Transactional(propagation = Propagation.NOT_SUPPORTED)
    void testCreateRecord() {
        OrchestratorEvent job = oxyJobSubmissionService.submitNewRequest(
                new JobRequest(SampleJobType.SAMPLE_PROCESSOR, UUID.randomUUID().toString(), Map.of()),
                "http://callback-url"
        );

        assertThat(job.getAttemptCount()).isEqualTo(0);
    }
 */
}